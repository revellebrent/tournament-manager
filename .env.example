 # Example environment variables for Tournament Manager
 # Copy this file to `.env.local` or `.env.production` and set real values.
 # Do NOT commit real secrets or API keys.

 # Base URL for the API the client will call. Must start with http(s):// and
 # be reachable from the browser (no private/internal hostnames unless proxied).
 VITE_API_BASE_URL=https://api.tournament-manager.example.com
